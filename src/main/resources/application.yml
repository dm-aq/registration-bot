spring:
  application:
    name: registration-bot
  datasource:
    url: ${JDBC_DATABASE_URL}
    username: ${JDBC_DATABASE_USERNAME}
    password: ${JDBC_DATABASE_PASSWORD}
    hikari:
      connection-test-query: SELECT 1
      validation-timeout: 3500
      minimum-idle: 0
      max-lifetime: 1200000
      idle-timeout: 13000
      maximum-pool-size: 15
      connection-timeout: 20000
      driver-class-name: org.postgresql.Driver
      pool-name: main
    logging: true
  liquibase:
    enabled: true
    drop-first: false
    change_log: classpath:liquibase/changelog-master.yaml

bot:
  username: ${BOT_USERNAME}
  token: ${BOT_TOKEN}

room:
  categories:
    2:
      price: 2500
      description: Двухместный номер
    3:
      price: 1700
      description: Трехместный номер
    4:
      price: 1400
      description: Четырехместный номер

dance-styles:
  values:
    - хастл
    - wcs
    - сальса
    - буги-вуги

users-allow:

services:
  sheets:
    spread-sheet-id: "1qdZpcyTdCnNFsexLeMDvmmjgNhgqcITwXjXvdu2fW40"
    range: "List1!A2:D"
